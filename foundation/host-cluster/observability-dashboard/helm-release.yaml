apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: observability-dashboard
  namespace: observability-dashboard
spec:
  interval: 5m
  chart:
    spec:
      chart: observability-dashboard
      version: '0.3.0'
      sourceRef:
        kind: HelmRepository
        name: agentic-layer
  values: {}
