apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: testbench
  namespace: testkube
spec:
  interval: 5m
  chart:
    spec:
      chart: testbench
      version: '0.8.2'
      sourceRef:
        kind: HelmRepository
        name: agentic-layer
  values: {}
  dependsOn:
    - name: testkube
